# 章节主图

每章一张主图，文件名与章节编号对齐。

| 源（可选） | 站点引用的 SVG |
| --- | --- |
| `01-map.excalidraw` | `../public/diagrams/01-map.svg` |
| `02-setup.excalidraw`（可补） | `../public/diagrams/02-setup.svg` |
| … | … |
| `08-pitfalls.excalidraw`（可补） | `../public/diagrams/08-pitfalls.svg` |

当前 02–08 以手写 SVG 交付（风格对齐 `01-map.svg`）。若用 Excalidraw 编辑，导出 SVG 到 `../public/diagrams/` 并保持同名。

统一风格：浅底、少颜色（黄=步骤、绿=完成/证据、蓝=信息、红=坑）、节点从左到右表示顺序；图上文字用中文短标签。
