# Excalidraw 源文件

每章一张主图，文件名与章节编号对齐：

- `01-map.excalidraw`
- `02-setup.excalidraw`
- …
- `08-pitfalls.excalidraw`

导出 SVG 到 `../public/diagrams/`，保持同名（如 `01-map.svg`）。

统一风格建议：浅底、少颜色、节点从左到右或上到下表示时间顺序；图上文字用中文短标签。
