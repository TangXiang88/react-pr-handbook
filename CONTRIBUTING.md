# 贡献指南

感谢你愿意改进这份手册。

## 欢迎直接 PR 的改动

- 错别字、病句、坏链
- 示意图标注错误、导出图与源文件不同步
- 示例仓库的 bug、类型错误、跑不起来
- **补充**降级场景、反例、检查清单（不推翻现有默认路径）

## 需要先 Discussion 的改动

- 改目录约定、PR 必填项、默认工具链等「强观点」
- 大幅重写某章主路径
- 新增整章（尤其是 v2 范围：状态管理、性能、组件库等）

开 Discussion 说明：想改什么、为什么、对初级读者的影响。维护者裁决后再动手改文档。

## 本地开发

```bash
npm install
npm run docs:dev
npm run app:dev
```

改主图时：编辑 `docs/diagrams/*.excalidraw`，导出 SVG 到 `docs/public/diagrams/`，并在对应章节引用。

## 许可

贡献代码即同意按 MIT 授权；贡献文档/图即同意按 CC BY 4.0 授权。
