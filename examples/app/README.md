# examples/app

教具级 React 应用（Vite + TypeScript），配合手册练习「小改动 → 规范 PR」。

## 目录

- `src/pages/` — 页面编排
- `src/features/` — 业务切片（当前：`tasks`）
- `src/components/ui/` — 共享哑组件

## 命令

在仓库根目录：

```bash
npm install
npm run app:dev
npm run app:build
```
